package JAVARuntime;

/** @Author Lucas Leandro (ITsMagic Founder) */
@ClassCategory(cat = {"Math"})
public class Random {

  private Random() {}

  /// Abstracts
  @JRDoc_EN("Returns a random integer value between min and max.")
  @JRDoc_PT("Retorna um valor inteiro aleatório entre o valor min e max.")
  @MethodArgs({"min", "max"})
  public static int range(int min, int max) {

    return 0;
  }

  @JRDoc_EN("Returns a random float value between min and max.")
  @JRDoc_PT("Retorna um valor float aleatório entre o valor min e max.")
  @MethodArgs({"min", "max"})
  public static float range(float min, float max) {

    return 0;
  }
}
